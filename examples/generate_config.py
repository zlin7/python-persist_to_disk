import persist_to_disk as ptd

ptd.config.generate_config()
# You can now edit the config file at ~/.persist_to_disk/config.ini
# Specifically, you can change the default caching path (`persist_path`).